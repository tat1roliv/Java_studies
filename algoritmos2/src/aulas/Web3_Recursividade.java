package aulas;

public class Web3_Recursividade {
	
/*
 Resolucao a partir de uma instancia conhecida;
 (Base da recursão)
 (contem no corpo da funcao uma chamada para si mesma)
 */

	/*
 	public static int nFatorial(int n) {
 		//base da recursao (condicao de parada)
		if (n==0){
			return 1;
		}else{
			return n * nFatorial(n-1);
		}
	}
 */
}
